spring:
    application:
        name: zero-djl-boot
    servlet:
        multipart:
            max-file-size: 100MB
            max-request-size: 100MB
    mvc:
        pathmatch:
            matching-strategy: ant_path_matcher